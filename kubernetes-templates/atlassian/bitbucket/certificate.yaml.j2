---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: bitbucket-cert
spec:
  commonName: {{ bitbucket_dns }}
  secretName: bitbucket-cert
  dnsNames:
    - {{ bitbucket_dns }}
  issuerRef:
    name: {{ cert_manager_issuer }}
    kind: ClusterIssuer
