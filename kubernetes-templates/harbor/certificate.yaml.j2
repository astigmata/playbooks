---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: harbor-cert
spec:
  commonName: {{ harbor_dns }}
  secretName: harbor-cert
  dnsNames:
    - {{ harbor_dns }}
  issuerRef:
    name: {{ cert_manager_issuer }}
    kind: ClusterIssuer
